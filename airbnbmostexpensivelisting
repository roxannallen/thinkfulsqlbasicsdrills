SELECT
	id,
	price,
	name,
	host_name,
	neighbourhood,
	room_type
FROM 
	sfo_listings
ORDER BY price DESC
LIMIT 1
--The most expensive listing is id#24650875, which is a full, 4-floor Victorian house in the Western Addition for $10,000 hosted by Sarah.